import { Meta, IconGallery, IconItem } from '@storybook/blocks';
import { iconNames, Icon } from "../components";

<Meta title="Powerhouse/Iconography" />

# Iconography

<IconGallery>
  {iconNames.map((iconName) => (
    <IconItem name={iconName} key={iconName}>
      <Icon name={iconName} />
    </IconItem>
  ))}
</IconGallery>
