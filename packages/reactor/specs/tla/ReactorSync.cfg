\* TLC Model Configuration for ReactorSync
\* Phase 1: Small model for quick validation

\* Initial state and next state
INIT Init
NEXT Next

\* Constants - Phase 1 bounds
CONSTANTS
    Reactors = {r1, r2}
    Documents = {doc1}
    MaxOperations = 3

\* Invariants to check (safety properties)
INVARIANTS
    TypeOK
    OperationUniqueness
    StreamMonotonicity
    CursorMonotonicity

\* Temporal properties to check (liveness/eventual properties)
\* PROPERTIES
\*     EventualConsistency

\* State space constraint for faster exploration
CONSTRAINT
    StateConstraint
