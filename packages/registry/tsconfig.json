{
  "extends": "../../tsconfig.options.json",
  "compilerOptions": {
    "outDir": "./dist",
    "types": ["node"],
  },
  "include": ["**/*"],
  "exclude": ["dist", "node_modules", "storage", "cdn-cache"],
  "references": [
    {
      "path": "../shared"
    }
  ]
}
