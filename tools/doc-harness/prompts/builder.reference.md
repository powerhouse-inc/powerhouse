
## Reference implementation

A reference implementation exists at `{{referenceDir}}`. You may read it. The docs remain the primary source: consult them first and use the reference to confirm or settle what they leave open. The `## Documentation gaps` section must still name what the docs lacked, even when the reference answered it.
