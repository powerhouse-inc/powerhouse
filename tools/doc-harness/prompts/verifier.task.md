# Findings to verify

Attempt: task `{{taskId}}`, arm `{{arm}}`.

{{findings}}

Report one result per index above, in order, with `prediction` written before `observation`. Return the structured object only.
