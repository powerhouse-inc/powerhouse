{
  "name": "doc-harness",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "Validates the Academy reactor docs by having docs-only Claude agents build recipes, then judging where the docs failed them.",
  "scripts": {
    "cli": "tsx src/cli.ts",
    "test": "vitest run",
    "tsc": "tsc",
    "lint": "eslint",
    "studio": "DOC_HARNESS_ROOT=$PWD mastra dev --dir src/mastra"
  },
  "dependencies": {
    "@mastra/core": "catalog:",
    "@mastra/libsql": "catalog:",
    "commander": "catalog:",
    "marked": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "mastra": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
