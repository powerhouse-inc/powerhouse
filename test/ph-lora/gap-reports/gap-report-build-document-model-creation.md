## Gap Report: Build — Document Model Creation

Reviewed: docs/academy/03-Build/02-DocumentModelCreation
Against: packages/document-model, packages/codegen, packages/builder-tools
Focus: Document model schema format, codegen output shape, reducer function signatures, generated file paths, document model versioning

### Findings

| # | Urgency | Type | Doc location | Source location | Finding |
|---|---------|------|-------------|-----------------|---------|
| 1 | high | wrong | `04-UseTheDocumentModelGenerator.md` "Understanding the Generated Artifacts" (lines 43-72) and "Practical Examples" (lines 150-153, 178-182) | `packages/codegen/src/file-builders/document-model/document-model.ts:128-134` | Doc presents the **non-versioned** layout (`document-models/todo-list/gen/`, `document-models/todo-list/src/reducers/todos.ts`) as the standard generator output, with versioning as an opt-in alternative. The generator ALWAYS emits a versioned layout: every spec writes to `document-models/<model>/v{version}/{gen,src,tests}/...` (`versionDirPath = join(documentModelDirPath, "v"+version)`, `srcDirPath = join(versionDirPath, "src")`, `genDirPath = join(versionDirPath, "gen")`). There is no code path that produces `gen/`/`src/` directly under the model dir. A developer looking for `document-models/todo-list/src/reducers/todos.ts` will not find it; it is at `document-models/todo-list/v1/src/reducers/todos.ts`. |
| 2 | medium | wrong | `04-UseTheDocumentModelGenerator.md` §1 "Specification Files" (lines 50-52) and lines 151, 179 | `packages/codegen/src/file-builders/document-model/document-model.ts:414` (json) and `packages/codegen/src/codegen/graphql.ts:207` (schema.graphql) | Doc places both `todo-list.json` and `schema.graphql` together at the model root (`document-models/todo-list/`). Source writes the JSON at the model root (`join(documentModelDirPath, documentModelDirName+".json")` → `document-models/todo-list/todo-list.json`, correct) but writes `schema.graphql` inside the version folder (`join(versionDirPath, "schema.graphql")` → `document-models/todo-list/v1/schema.graphql`), not at the model root. The two files do not live side by side. |
| 3 | low | stale | `04-UseTheDocumentModelGenerator.md` "Standard vs Versioned" table (lines 110-115) | `packages/codegen/src/file-builders/document-model/document-model.ts:128-134` | The table contrasts "Standard Generation: Single `gen/` and `src/` folder" against "Versioned Generation: Separate `v1/`, `v2/` folders". A standard (non-versioned, single `gen/`+`src/`) generation mode does not exist in the generator — output is always versioned. The contrast is misleading. |

### Verified clean

- `generateId` — exists as `export function generateId(method?: "UUIDv4"): string` at `packages/shared/document-model/utils.ts:4`; docs call `generateId()` with no args (valid). Re-exported via `document-model` root and `document-model/core` (both subpaths resolve to `index.ts` per `packages/document-model/package.json` exports). Import path `document-model/core` confirmed valid.
- `generateMock` — exists as `export function generateMock<TSchema extends z.ZodType>(...)` at `packages/shared/document-model/mock.ts:4`, re-exported by `utils.ts:1`. Doc 06 imports it from `document-model` and calls `generateMock(AddTodoItemInputSchema())` — shape matches.
- Reducer operations interface name — doc's `TodoListTodosOperations` matches generated `${pascalCaseDocumentType}${pascalCaseModuleName}Operations` (`packages/codegen/src/templates/document-model/gen/modules/operations.ts:18-24` and `packages/codegen/src/file-builders/document-model/src-dir.ts:62`).
- Reducer method names — doc's `addTodoItemOperation` / `updateTodoItemOperation` / `deleteTodoItemOperation` match generated `${camelCase(operation.name)}Operation` (`packages/codegen/src/templates/document-model/gen/modules/operations.ts:28-30`; src-dir.ts:139-141 builds `${camelCase(operation.name)}Operation`).
- Reducer method signature — doc's `(state, action)` matches generated parameters `[{ name: "state" }, { name: "action" }]` (`packages/codegen/src/file-builders/document-model/src-dir.ts:152`).
- `gen/` file inventory — `types.ts`, `creators.ts`, `utils.ts`, `reducer.ts` all generated (`packages/codegen/src/file-builders/document-model/gen-dir.ts:94,158,56,228`), matching doc 04 §2.
- `utils.createDocument()` — generated `createDocument(state)` method exists in gen utils template (`packages/codegen/src/templates/document-model/gen/utils.ts:67`); doc 06 usage `utils.createDocument()` / `utils.createDocument({ global: { items } })` consistent.
- Action creators — gen index re-exports creators (`export * from './creators.js'`, `packages/codegen/src/templates/document-model/gen/index.ts`), so `addTodoItem`/`updateTodoItem`/`deleteTodoItem` and `reducer`, `utils` are available from the model entry; doc 06 imports consistent.
- `is<Type>Document` helper — generated via `isPhDocumentOfTypeFunctionName` (`packages/codegen/src/templates/document-model/gen/utils.ts:54,89-90`); doc 06's `isTodoListDocument` consistent.
- Versioned `upgrades/` files — `versions.ts` (exports `supportedVersions`, `latestVersion`), `v{version}.ts`, `upgrade-manifest.ts` all generated (`packages/codegen/src/file-builders/document-model/upgrades-dir.ts`: `createOrUpdateVersionConstantsFile` line ~99, `makeUpgradeFile` line 13, `createOrUpdateUpgradeManifestFile` line 31). Matches doc 04 (lines 90-106) and doc 08 (lines 70-97) folder structure.
- `versions.ts` contents — doc 08 lines 152-154 (`export const supportedVersions = [1, 2] as const;` / `export const latestVersion = supportedVersions[1];`) matches generator output (`upgrades-dir.ts` `versionInitializer = "["+versions.join(", ")+"] as const;"`, `latestInitializer = supportedVersions[index]`).
- `document-models.ts` aggregate — generated at `document-models/document-models.ts` (`document-model.ts:236` `makeDocumentModelsFile`); doc 04 line 105 and doc 08 lines 248-259 consistent.
- Per-version `module.ts` exporting `DocumentModelModule` with `version` — `makeDocumentModelModuleFile` writes `v{version}/module.ts` (`document-model.ts:171`); `DocumentModelModule<TState>` type at `packages/shared/document-model/types.ts:1694` has optional `version?: number`, `reducer`, `actions`, `utils`, `documentModel` fields — matches doc 08 lines 168-186.
- `UpgradeReducer` type — `export type UpgradeReducer<TFrom, TTo> = (document: PHDocument<TFrom>, action: Action) => PHDocument<TTo>` at `packages/shared/document-model/upgrades.ts:8-11`; doc 08 lines 198-219 upgrade reducer signature `(document: PHDocument<StateV1>, action: Action): PHDocument<StateV2>` matches.
- `UpgradeTransition` type — `{ toVersion: ModelVersion; upgradeReducer: UpgradeReducer<any, any>; description?: string }` at `packages/shared/document-model/upgrades.ts:15-19`; doc 08 lines 109-113 (`toVersion: number; upgradeReducer: UpgradeReducer<any, any>; description?: string`) matches.
- `UpgradeManifest<TVersions>` type — `{ documentType; latestVersion; supportedVersions; upgrades }` at `packages/shared/document-model/upgrades.ts:24-35`; doc 08 lines 122-131 matches (mapped key `v${V}` excluding 1 matches source exactly).
- `PHDocument`, `Action`, `createState`, `defaultBaseState` — all exist: `PHDocument` at `packages/shared/document-model/documents.ts:112`, `createState` at `state.ts:163`, `defaultBaseState` at `state.ts:31`; doc 08 imports from `document-model` / `document-model/core` valid.
- Doc 03's reducer import `../../gen/todos/operations.js` (line 109) — resolves correctly from the generated reducer location `v1/src/reducers/todos.ts` up to `v1/gen/todos/operations.js`, which is generated. Not drift.
- GraphQL SDL schema format, custom scalar `OID`, `input`-type operation definitions (docs 01-03) — consistent with GraphQL SDL authoring; no source contradiction within scope.

### Could not verify

- `ph generate document-model --document <file.phd>` vs `ph generate TodoList.phd` — doc 04 lines 28-35 use the long form, while doc 04 line 83, doc 05 line 19, and doc 08 line 142 use the short `ph generate TodoList.phd`. CLI command/flag shape lives in `clis/ph-cli` / `clis/ph-cmd`, which is outside this section's checkFocus and was not loaded; the exact accepted argument form for `ph generate` cannot be anchored to source here. (Belongs to `reference-cli` / `get-started`.)
- `todo-tutorial/document-models/todo-list` import alias used throughout docs 03/05/06 — this is a project-package-name-scoped path alias dependent on the generated project's `package.json` name, not a codegen-emitted literal; whether it resolves depends on the example repo's package name, which was not in scope.
- Vetra Studio / Connect UI flows (buttons, "Sync with schema", "Add module") in docs 02/03/04 — UI behaviour requires runtime verification (builder-tools/connect rendering), outside static check.
- Doc 06 `operations.global[0].action.type` / `.action.input` / `.index` operation-record shape — relies on runtime `reducer` output structure; PHDocument operation entry shape not loaded in detail during this review.

### Summary

3 findings (1 stale, 0 missing, 2 wrong). The conceptual schema/operations/reducer/versioning content and all exported names (generateId, generateMock, DocumentModelModule, UpgradeReducer/Transition/Manifest, reducer interface and method signatures) match source cleanly; the drift is concentrated in `04-UseTheDocumentModelGenerator.md`, which describes a non-versioned generated file layout and `schema.graphql` location that the generator no longer produces — output is always versioned under `v{version}/`.
