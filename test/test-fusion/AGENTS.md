<!-- BEGIN:nextjs-agent-rules -->
# Next.js version note

This is stock Next.js (16.2.10). The only customizations are in the
app code, which has been changed from the `create-next-app` boilerplate. But this
Next.js version is newer than your training data: APIs and conventions may differ,
so read the relevant guide in `node_modules/next/dist/docs/` before writing code,
and heed deprecation notices.
<!-- END:nextjs-agent-rules -->
