// A reactor package with no pieces at all: the shape every generated project
// has had since before `ph build` learned about pieces.
export { documentModels } from "./document-models/index.js";
export { editors } from "./editors/index.js";
