document-models
editors 
processors
*.zip
*.db
.ph
src/version.ts
build/
# built output of the test fixtures ph build runs against
test/fixtures/*/dist
test/fixtures/*/*.tsbuildinfo
# the fixtures are source, even where they use the generated directory names
!test/fixtures/*/document-models
