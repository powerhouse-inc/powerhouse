overwrite: true
schema: "./subgraphs/*/*.graphql"
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript-common"
  ./graphql.schema.json:
    plugins:
      - "introspection"
