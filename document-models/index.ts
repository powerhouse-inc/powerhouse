/**
 * This is a scaffold file meant for customization.
 * Delete the file and run the code generator again to have it reset
 */
import { module as BudgetStatement } from './budget-statement';
import { module as ScopeFramework } from './scope-framework';

export const documentModels = [ScopeFramework, BudgetStatement];
